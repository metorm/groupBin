name: groupbin
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - flask
      - flask-sqlalchemy
      - flask-login
      - werkzeug
      - python-dotenv
      - flask-cors
      - gunicorn
      - pycryptodome
      - python-multipart
      - tqdm
      - flask-wtf
